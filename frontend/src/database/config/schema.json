{
  "version": "1.1.0",
  "generatedAt": "2025-07-15T10:30:00Z",
  "database": {
    "tables": {
      "recipes": {
        "columns": {
          "id": "TEXT PRIMARY KEY",
          "title": "TEXT NOT NULL",
          "avg_rating": "REAL DEFAULT 0"
        },
        "indexes": [
          "CREATE INDEX idx_recipes_rating ON recipes(avg_rating)"
        ]
      },
      "user_favorites": {
        "columns": {
          "id": "INTEGER PRIMARY KEY AUTOINCREMENT", 
          "recipe_id": "TEXT NOT NULL"
        },
        "foreign_keys": [
          "FOREIGN KEY (recipe_id) REFERENCES recipes(id)"
        ]
      }
    }
  }
}